# Design log

Each user-visible change lands in its own commit and adds a pending entry here.
Only a human can approve or reject an entry. Rejected work is reverted, and its
entry remains with a `reverted` status.

## Log

- 2026-08-26 · pending · Create the Paper Tailwind to StyleX converter with Million UI.

## Backlog

- Host the Million UI registry and replace the local registry URL.
- Add the licensed Berkeley Mono file when it can be distributed to this project.
- Install the Million UI copy mark after `CENTRAL_LICENSE_KEY` is available.
